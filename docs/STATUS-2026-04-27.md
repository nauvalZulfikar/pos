# DESAIN POS — Status Report (2026-04-27)

**Live deploy:** https://pos.aureonforge.com (admin/, /api, /kasir)
**Repo:** https://github.com/nauvalZulfikar/pos.git
**Build:** ✅ typecheck green, ✅ 122+ tests pass, ✅ admin + pos production builds OK
**Stack on VPS (72.60.196.21):** pm2 (4 apps) · Postgres 16 · Redis 7 · nginx + Let's Encrypt

---

## What changed in this session

### POS terminal (kasir)
| Feature | Before | Now |
|---|---|---|
| Discount apply (percent/nominal + alasan) | UI cut | ✅ `DiscountModal` in OrderEntry, audit on submit |
| Void item (alasan wajib) | UI cut | ✅ `VoidModal` in OrderEntry |
| Send to Kitchen | UI cut | ✅ Tombol di OrderEntry → POST /send-to-kitchen |
| Pay screen (Cash/QRIS/EDC) | UI cut | ✅ `Payment.tsx` with tendered+kembalian, QRIS poll, mock-settle, EDC last-4 |
| Cetak struk | UI cut | ✅ `/v1/orders/:id/text` → window.print() |
| Shift open/close + cash count | dummy form | ✅ Real `/v1/shifts/*` API + variance display |

### Admin dashboard
| Feature | Before | Now |
|---|---|---|
| Inventory adjust per outlet | API only | ✅ `/inventory/adjust` form (outlet + item + delta + reason) |
| Customer detail + order history | list only | ✅ `/customers/[id]` with loyalty card + visit stats |
| Order admin + refund | missing | ✅ `/orders` list + `/orders/[id]` with refund button |
| Peak Hour analytics | missing | ✅ `/reports/peak-hours` with hour-bucket chart + top 3 picks |
| Order nav link | missing | ✅ Added to sidebar |

### Backend / business logic
| Feature | Before | Now |
|---|---|---|
| Loyalty tier auto-upgrade | static `'regular'` | ✅ regular/silver/gold based on visit_count, no downgrade |
| Voucher schedule (happy hour) | accept always | ✅ JSON `{daysOfWeek, start, end}` in Asia/Jakarta tz |
| Voucher redemption tracking | not recorded | ✅ Inserts into `voucher_redemptions`, increments `usedCount` atomically |
| `/v1/payments/by-order/:orderId` | missing | ✅ Added (used by admin order detail) |
| PII at-rest encryption | TODO comment | ✅ AES-256-GCM via `PII_ENCRYPTION_KEY_HEX`, plaintext fallback w/ warning |
| `/v1/reports/peak-hours` | missing | ✅ Hour-of-day bucketing in Asia/Jakarta, fills 24 slots, returns top 3 |

### Production deployment (new!)
- VPS at 72.60.196.21 with code in `/root/projects/pos/`
- pm2 ecosystem: 4 long-running services (api, worker, admin, pos PWA)
- Dedicated postgres + redis containers (ports 25435/26381) — no collision with jobflow/sibedas
- nginx site `pos.aureonforge.com` with TLS auto-renewal
- Migrations + RLS + audit triggers + seed all applied

---

## Honest tag count update

| Status | This session | Previous |
|---|---|---|
| ✅ Fully working | **27** | 18 |
| 🟢 Functional but cosmetic gaps | **9** | 16 |
| 🟡 Stub / mock / partial | **2** | 6 |
| ⛔ Blocked on external creds | **4** | 4 |

### ✅ Fully working (27)
PIN login, table mgmt, menu CRUD, categories CRUD, modifiers CRUD, recipe linking, order entry, **void**, **discount**, **send-to-kitchen**, **payment (Cash+QRIS+EDC)**, **print receipt**, **shift open/close + cash variance**, KDS, sync engine, audit log, customer CRUD + **detail + order history**, inventory list + create + **adjust**, **outlet CRUD**, **staff invite + PIN**, audit viewer, daily report, sales trend, top items, **peak hours**, **order admin + refund**, multi-outlet overview, **voucher CRUD + redemption + happy-hour schedule**, signup flow, **PII encryption at rest**, **loyalty tier auto-upgrade**, R2-or-local uploads, recipe auto-deduct worker, anomaly worker, daily-brief worker (with deterministic fallback), supplier CRUD.

### 🟢 Functional but cosmetic gaps (9)
- Hold order / split bill (POS UI not built — backend supports drafts via order status)
- Per-outlet permission overrides (DB+middleware ready, no admin UI)
- Voucher redeem in POS UI (admin-only currently)
- Tables drag-drop editor (basic CRUD only)
- Multi-outlet inventory transfer (single-outlet adjust only)
- Email signup verification (token issuance ready, no SMTP wiring)
- E-Faktur CSV export (data flow OK, UI export button missing)
- Marketing site (Next 15 scaffold exists, content not authored)
- ML service (Python FastAPI scaffold exists, models not trained)

### 🟡 Stub / mock / partial (2)
- WhatsApp outbound — queue + worker present, Meta API not wired (no creds)
- Delivery platform inbound (GoFood/Grab/Shopee) — webhook handlers parse payloads but no real signature validation (no creds)

### ⛔ Blocked on external creds (4)
- Midtrans QRIS production (sandbox key works; mock-settle dev endpoint covers demos)
- WhatsApp Business API (Meta access token)
- GoFood/Grab/Shopee partner API (each platform's creds + signing key)
- Resend (transactional email — for signup verify + receipts)

---

## What's still on the dev backlog

1. **Hold/split-bill UI** — backend already supports multi-payment-per-order; just needs POS UI flow
2. **Voucher redemption in POS** — admin can create vouchers but kasir can't apply them yet
3. **Per-outlet permission override UI** — model exists, no settings panel
4. **Ops runbooks** — 4 incident docs (DB failover, Midtrans outage, breach response, mass tenant breach)
5. **More test coverage** — current 122 tests cover happy path; void/discount/refund/voucher/loyalty earn-redeem need targeted unit tests

---

## Pipeline status

```
typecheck   ✅ all 14 packages green
test        ✅ 122 tests pass
build:admin ✅ Next.js 15 production build OK (pos.aureonforge.com)
build:pos   ✅ Vite + Workbox PWA built (467 kB → 140 kB gzip)
api runtime ✅ Hono + tsx live on 23000
worker      ✅ pm2 online, BullMQ workers idle waiting jobs
nginx       ✅ HTTP→HTTPS redirect, /api proxy, /kasir proxy
TLS         ✅ Let's Encrypt (renews automatically)
DNS         ✅ pos.aureonforge.com → 72.60.196.21
```

## Demo creds

```
URL:           https://pos.aureonforge.com
Owner login:   admin@desain.id  / admin123
Kasir PIN:     1234
Outlet:        00000000-0000-7000-8000-000000000002 (seeded)
```
